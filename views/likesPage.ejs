<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Likes - KravN</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/js/theme.js"></script>
</head>
<body>
  <div class="topbar">
    <div class="topbar-inner">
      <a class="brand" href="/feed">Krav<span>N</span></a>
      <div class="nav-links">
        <a class="muted" href="/posts/<%= post.id %>">← Back to post</a>
      </div>
    </div>
  </div>

  <div class="page-narrow">
    <h1 style="font-size: 1.4rem; margin-top: var(--space-6); margin-bottom: 0.25rem;">People who liked this</h1>
    <p class="auth-subtitle" style="margin-bottom: 1.5rem;"><%= post.movie_name %>: <%= post.title %></p>

    <% if (likers.length === 0) { %>
      <div class="empty-state"><p>No likes yet.</p></div>
    <% } %>

    <% likers.forEach(user => { %>
      <div style="padding: 0.75rem 0; border-bottom: 1px solid var(--border); display: flex; align-items: center; gap: 0.75rem;">
        <div class="avatar-placeholder">
          <%= user.username[0].toUpperCase() %>
        </div>
        <a href="/users/<%= user.username %>"><%= user.username %></a>
      </div>
    <% }); %>
  </div>
</body>
</html>
